# Placeholder assets

当前 PR 平台不支持二进制文件预览，因此视觉 Demo 暂不提交 PNG/JPG 等二进制占位图。

首页里的植物、饮食、运动、五音图形均由 `src/styles/home.css` 中的 CSS 占位插画绘制，依然不依赖任何网络图片。

后续接入正式设计资源时，可在此目录放入真实图片，并把组件中的 CSS 占位插画替换为 `<img>` 或框架资产引用。
